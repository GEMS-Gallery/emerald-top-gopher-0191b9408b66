type Time = int;
type Task = 
 record {
   completed: bool;
   completedAt: opt Time;
   description: text;
   id: nat;
 };
service : {
  addTask: (text) -> (nat);
  completeTask: (nat) -> (bool);
  deleteTask: (nat) -> (bool);
  getTasks: () -> (vec Task) query;
}
